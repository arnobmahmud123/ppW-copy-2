import React from "react";

interface MediaViewerEditorProps {
  [key: string]: any;
}

export function MediaViewerEditor(props: MediaViewerEditorProps) {
  return <div>Media Viewer Editor (Stub)</div>;
}
